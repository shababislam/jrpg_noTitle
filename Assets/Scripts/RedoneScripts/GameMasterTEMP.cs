using UnityEngine;
using System.Collections;
/*
public class GameMasterTEMP : MonoBehaviour {

	public static bool GamemasterTEMPExists;

	public static int currentQuest;

	public static bool questInProgress = false;

	public static bool inParty = true;

	public GameObject[] currentParty;

	/*
		TO DO:
		- add the QuestManager here and work out how you fixed it, and whether it works with choices

		IDEAS:
		- possibly have an array of ALL playable characters all loaded up
		- for moving cameras, you can make the NEW destination the parent of the camera object 
		  and THEN set the position/local variables

	*/
/*
	void Start () {
		if(!GamemasterTEMPExists){
			GamemasterTEMPExists = true;
			GameObject.DontDestroyOnLoad(this);
		} else {
			Destroy(gameObject);
		}
	}
	
	void Update () {
		Debug.Log("questnumber: "+currentQuest+", in progress: "+questInProgress);
	}


}
*/